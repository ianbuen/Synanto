<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/invite_userID"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextViewAppearance"
        android:textSize="24sp" />

    <TextView
        android:id="@+id/invite_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/invite_userID"
        android:layout_marginStart="10dp"
        android:layout_alignBottom="@id/invite_userID"
        android:layout_toRightOf="@+id/invite_userID"
        android:layout_marginLeft="10dp"
        android:textAppearance="@style/TextViewAppearance"
        android:textSize="24sp" />

</RelativeLayout>